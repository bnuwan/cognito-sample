version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd cognito-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: cognito-app/dist
    files:
      - '**/*'
  cache:
    paths:
      - cognito-app/node_modules/**/*
